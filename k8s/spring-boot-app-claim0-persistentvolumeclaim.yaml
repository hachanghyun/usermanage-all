apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: spring-boot-app-claim0
  name: spring-boot-app-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
