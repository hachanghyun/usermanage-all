apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: logstash-claim1
  name: logstash-claim1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
